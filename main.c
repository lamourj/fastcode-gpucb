#include "stdio.h"
#include "baseline.c"

void learn(){
    /*
     * grid_idx = self.argmax_ucb()
    *  self.sample(self.X_grid[grid_idx])
    *  for every point x:
     *  gp_regression()
    *  gp.fit(self.X, self.T)
    *  mu1 = self.mu
     */
}

int main(){
    printf("Welcome\n");


    for(int k=0; k<20; k++){
        learn();
    }
    return 0;
}
